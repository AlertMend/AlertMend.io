{
  "name": "alertmend-mock-app",
  "private": true,
  "version": "0.1.0",
  "description": "Self-contained mock of the AlertMend product UI (React frontend + Express backend) that reuses the marketing site's design tokens.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c magenta,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
